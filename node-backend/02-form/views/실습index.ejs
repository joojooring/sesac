<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
  </head>
  <body>

    <form action="/post" method="post" target="_blank" name="login">
      이름<input type="text" name="id">


      <fieldset>
        <legend>성별</legend>
        <input type="radio" name="gender" value="man">남자
        <input type="radio" name="gender" value="woman">여자

      </fieldset>

      <fieldset>
        <legend> 생년월일</legend>

        <select name="year">
          <% for(let i = 1900; i<=2100 ; i++){%>
          <option value="<%=i %>"><%=i %></option>

          <%} %>
        </select>년

        <select name="month">
          <% for(let i = 1; i<=12 ; i++){%>
          <option value="<%=i %>"><%=i %></option>월

          <%} %>
        </select>월

        <select name="day">
          <% for(let i = 1; i<=31 ; i++){%>
          <option value="<%=i %>"><%=i %></option>일

          <%} %>
        </select>일

        <!-- <input type = "select" name="birth" max="9999" min="1900">년
                    <input type = "select" name="birth" max="12" min="1" >월
            <input type = "select" name="birth" max="31" min="1">일 -->
        </label>
      </fieldset>

      <fieldset>
        <legend> 관심사</legend>
        <input type="checkbox" name="interst" value="travel">여행
        <input type="checkbox" name="interst" value="fashion">패션
        <input type="checkbox" name="interst" value="food">음식
        <input type="checkbox" name="interst" value="music">음악


      </fieldset>

      <fieldset>
        <legend>선택</legend>
        좋아하는 색상 <input type="color">
        <br />
        좋아하는 숫자 <input type="number">
      </fieldset>
      <br />
      <button type="submit">회원가입</button>

    </form>

  </body>
</html>